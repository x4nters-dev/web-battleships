export enum EventType {
    connected = 'connected',
    created = 'created',
    updated = 'updated',
    joined = 'joined',
    ready = 'ready',
    started = 'started',
    shot = 'shot',
    finished = 'finished',
    removed = 'removed'
}